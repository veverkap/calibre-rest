{
  "__init__": {
    "file": "__init__.py",
    "usage_text": null,
    "options": [],
    "option_groups": []
  },
  "cmd_add": {
    "file": "cmd_add.py",
    "usage_text": "%prog add [options] file1 file2 file3 ...\n\nAdd the specified files as books to the database. You can also specify folders, see\nthe folder related options below.\n",
    "options": [
      {
        "_file": "cmd_add.py",
        "action": "store_true",
        "default": false,
        "help": "Add books to database even if they already exist. Comparison is done based on book titles and authors. Note that the --automerge option takes precedence.",
        "flags": [
          "-d",
          "--duplicates"
        ]
      },
      {
        "_file": "cmd_add.py",
        "type": "choice",
        "choices": "('disabled', 'ignore', 'overwrite', 'new_record')",
        "default": "disabled",
        "help": "If books with similar titles and authors are found, merge the incoming formats (files) automatically into existing book records. A value of \"ignore\" means duplicate formats are discarded. A value of \"overwrite\" means duplicate formats in the library are overwritten with the newly added files. A value of \"new_record\" means duplicate formats are placed into a new book record.",
        "flags": [
          "-m",
          "--automerge"
        ]
      },
      {
        "_file": "cmd_add.py",
        "action": "store_true",
        "default": false,
        "help": "Add an empty book (a book with no formats)",
        "flags": [
          "-e",
          "--empty"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Set the title of the added book(s)",
        "flags": [
          "-t",
          "--title"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Set the authors of the added book(s)",
        "flags": [
          "-a",
          "--authors"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Set the ISBN of the added book(s)",
        "flags": [
          "-i",
          "--isbn"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": "[]",
        "action": "append",
        "help": "Set the identifiers for this book, e.g. -I asin:XXX -I isbn:YYY",
        "flags": [
          "-I",
          "--identifier"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Set the tags of the added book(s)",
        "flags": [
          "-T",
          "--tags"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Set the series of the added book(s)",
        "flags": [
          "-s",
          "--series"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": 1.0,
        "type": "float",
        "help": "Set the series number of the added book(s)",
        "flags": [
          "-S",
          "--series-index"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "Path to the cover to use for the added book",
        "flags": [
          "-c",
          "--cover"
        ]
      },
      {
        "_file": "cmd_add.py",
        "default": null,
        "help": "A comma separated list of languages (best to use ISO639 language codes, though some language names may also be recognized)",
        "flags": [
          "-l",
          "--languages"
        ]
      },
      {
        "_file": "cmd_add.py",
        "action": "store_true",
        "default": false,
        "help": "Assume that each folder has only a single logical book and that all files in it are different e-book formats of that book",
        "flags": [
          "-1",
          "--one-book-per-directory"
        ]
      },
      {
        "_file": "cmd_add.py",
        "action": "store_true",
        "default": false,
        "help": "Process folders recursively",
        "flags": [
          "-r",
          "--recurse"
        ]
      },
      {
        "_file": "cmd_add.py",
        "action": "callback",
        "type": "string",
        "nargs": 1,
        "default": "[]",
        "callback": "filter_pat",
        "dest": "filters",
        "callback_args": "(action,)",
        "metavar": "GLOB PATTERN",
        "help": "help"
      }
    ],
    "option_groups": []
  },
  "cmd_add_custom_column": {
    "file": "cmd_add_custom_column.py",
    "usage_text": "%prog add_custom_column [options] label name datatype\n\nCreate a custom column. label is the machine friendly name of the column. Should\nnot contain spaces or colons. name is the human friendly name of the column.\ndatatype is one of: ', '.join(sorted(CustomColumns.CUSTOM_DATA_TYPES))\n",
    "options": [
      {
        "_file": "cmd_add_custom_column.py",
        "default": false,
        "action": "store_true",
        "help": "This column stores tag like data (i.e. multiple comma separated values). Only applies if datatype is text.",
        "flags": [
          "--is-multiple"
        ]
      },
      {
        "_file": "cmd_add_custom_column.py",
        "default": "{}",
        "help": "A dictionary of options to customize how the data in this column will be interpreted. This is a JSON  string. For enumeration columns, use --display=\"{\\\"enum_values\\\":[\\\"val1\\\", \\\"val2\\\"]}\"\nThere are many options that can go into the display variable.The options by column type are:\ncomposite: composite_template, composite_sort, make_category,contains_html, use_decorations\ndatetime: date_format\nenumeration: enum_values, enum_colors, use_decorations\nint, float: number_format\ntext: is_names, use_decorations\n\nThe best way to find legal combinations is to create a custom column of the appropriate type in the GUI then look at the backup OPF for a book (ensure that a new OPF has been created since the column was added). You will see the JSON for the \"display\" for the new column in the OPF.",
        "flags": [
          "--display"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_add_format": {
    "file": "cmd_add_format.py",
    "usage_text": "%prog add_format [options] id ebook_file\n\nAdd the e-book in ebook_file to the available formats for the logical book identified by id. You can get id by using the search command. If the format already exists, it is replaced, unless the do not replace option is specified.",
    "options": [
      {
        "_file": "cmd_add_format.py",
        "dest": "replace",
        "default": true,
        "action": "store_false",
        "help": "Do not replace the format if it already exists",
        "flags": [
          "--dont-replace"
        ]
      },
      {
        "_file": "cmd_add_format.py",
        "default": false,
        "action": "store_true",
        "help": "Add the file as an extra data file to the book, not an ebook format",
        "flags": [
          "--as-extra-data-file"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_backup_metadata": {
    "file": "cmd_backup_metadata.py",
    "usage_text": "%prog backup_metadata [options]\n\nBackup the metadata stored in the database into individual OPF files in each\nbooks folder. This normally happens automatically, but you can run this\ncommand to force re-generation of the OPF files, with the --all option.\n\nNote that there is normally no need to do this, as the OPF files are backed up\nautomatically, every time metadata is changed.\n",
    "options": [
      {
        "_file": "cmd_backup_metadata.py",
        "default": false,
        "action": "store_true",
        "help": "Normally, this command only operates on books that have out of date OPF files. This option makes it operate on all books.",
        "flags": [
          "--all"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_catalog": {
    "file": "cmd_catalog.py",
    "usage_text": "%prog catalog /path/to/destination.(csv|epub|mobi|xml...) [options]\n\nExport a catalog in format specified by path/to/destination extension.\nOptions control how entries are displayed in the generated catalog output.\nNote that different catalog formats support different sets of options. To\nsee the different options, specify the name of the output file and then the\n--help option.\n",
    "options": [
      {
        "_file": "cmd_catalog.py",
        "default": null,
        "dest": "ids",
        "help": "Comma-separated list of database IDs to catalog.\nIf declared, --search is ignored.\nDefault: all",
        "flags": [
          "-i",
          "--ids"
        ]
      },
      {
        "_file": "cmd_catalog.py",
        "default": null,
        "dest": "search_text",
        "help": "Filter the results by the search query. For the format of the search query, please see the search-related documentation in the User Manual.\nDefault: no filtering",
        "flags": [
          "-s",
          "--search"
        ]
      },
      {
        "_file": "cmd_catalog.py",
        "default": false,
        "action": "store_true",
        "dest": "verbose",
        "help": "Show detailed output information. Useful for debugging",
        "flags": [
          "-v",
          "--verbose"
        ]
      },
      {
        "_file": "cmd_catalog.py",
        "default": "option.default",
        "dest": "option.dest",
        "action": "option.action",
        "help": "option.help"
      },
      {
        "_file": "cmd_catalog.py",
        "default": "option.default",
        "dest": "option.dest",
        "help": "option.help"
      }
    ],
    "option_groups": []
  },
  "cmd_check_library": {
    "file": "cmd_check_library.py",
    "usage_text": "%prog check_library [options]\n\nPerform some checks on the filesystem representing a library. Reports are ', '.join([c[0] for c in CHECKS])\n",
    "options": [
      {
        "_file": "cmd_check_library.py",
        "default": false,
        "action": "store_true",
        "help": "Output in CSV",
        "flags": [
          "-c",
          "--csv"
        ]
      },
      {
        "_file": "cmd_check_library.py",
        "default": null,
        "dest": "report",
        "help": "Comma-separated list of reports.\nDefault: all",
        "flags": [
          "-r",
          "--report"
        ]
      },
      {
        "_file": "cmd_check_library.py",
        "default": null,
        "dest": "exts",
        "help": "Comma-separated list of extensions to ignore.\nDefault: all",
        "flags": [
          "-e",
          "--ignore_extensions"
        ]
      },
      {
        "_file": "cmd_check_library.py",
        "default": null,
        "dest": "names",
        "help": "Comma-separated list of names to ignore.\nDefault: all",
        "flags": [
          "-n",
          "--ignore_names"
        ]
      },
      {
        "_file": "cmd_check_library.py",
        "default": false,
        "action": "store_true",
        "help": "Vacuum the full text search database. This can be very slow and memory intensive, depending on the size of the database.",
        "flags": [
          "--vacuum-fts-db"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_clone": {
    "file": "cmd_clone.py",
    "usage_text": "%prog clone path/to/new/library\n\nCreate a clone of the current library. This creates a new, empty library that has all the\nsame custom columns, Virtual libraries and other settings as the current library.\n\nThe cloned library will contain no books. If you want to create a full duplicate, including\nall books, then simply use your filesystem tools to copy the library folder.\n    ",
    "options": [],
    "option_groups": []
  },
  "cmd_custom_columns": {
    "file": "cmd_custom_columns.py",
    "usage_text": "%prog custom_columns [options]\n\nList available custom columns. Shows column labels and ids.\n    ",
    "options": [
      {
        "_file": "cmd_custom_columns.py",
        "default": false,
        "action": "store_true",
        "help": "Show details for each column.",
        "flags": [
          "-d",
          "--details"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_embed_metadata": {
    "file": "cmd_embed_metadata.py",
    "usage_text": "\n%prog embed_metadata [options] book_id\n\nUpdate the metadata in the actual book files stored in the calibre library from\nthe metadata in the calibre database.  Normally, metadata is updated only when\nexporting files from calibre, this command is useful if you want the files to\nbe updated in place. Note that different file formats support different amounts\nof metadata. You can use the special value 'all' for book_id to update metadata\nin all books. You can also specify many book ids separated by spaces and id ranges\nseparated by hyphens. For example: %prog embed_metadata 1 2 10-15 23",
    "options": [
      {
        "_file": "cmd_embed_metadata.py",
        "action": "append",
        "default": "[]",
        "help": "Only update metadata in files of the specified format. Specify it multiple times for multiple formats. By default, all formats are updated.",
        "flags": [
          "-f",
          "--only-formats"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_export": {
    "file": "cmd_export.py",
    "usage_text": "%prog export [options] ids\n\nExport the books specified by ids (a comma separated list) to the filesystem.\nThe export operation saves all formats of the book, its cover and metadata (in\nan OPF file). Any extra data files associated with the book are also saved.\nYou can get id numbers from the search command.\n",
    "options": [
      {
        "_file": "cmd_export.py",
        "default": false,
        "action": "store_true",
        "help": "Export all books in database, ignoring the list of ids.",
        "flags": [
          "--all"
        ]
      },
      {
        "_file": "cmd_export.py",
        "default": ".",
        "help": "_('Export books to the specified folder. Default is') + ' %default'",
        "flags": [
          "--to-dir"
        ]
      },
      {
        "_file": "cmd_export.py",
        "default": false,
        "action": "store_true",
        "help": "Export all books into a single folder",
        "flags": [
          "--single-dir"
        ]
      },
      {
        "_file": "cmd_export.py",
        "default": false,
        "action": "store_true",
        "help": "Report progress",
        "flags": [
          "--progress"
        ]
      },
      {
        "_file": "cmd_export.py",
        "default": true,
        "action": "store_false",
        "help": "opt.help + ' ' + _('Specifying this switch will turn this behavior off.')",
        "dest": "pref"
      },
      {
        "_file": "cmd_export.py",
        "default": "opt.default",
        "help": "opt.help",
        "dest": "pref"
      },
      {
        "_file": "cmd_export.py",
        "default": false,
        "action": "store_true",
        "help": "opt.help"
      }
    ],
    "option_groups": []
  },
  "cmd_fts_index": {
    "file": "cmd_fts_index.py",
    "usage_text": "%prog fts_index [options] {enable}/{disable}/{status}/{reindex}\n\nControl the Full text search indexing process.\n\n{enable}\n    Turns on FTS indexing for this library\n{disable}\n    Turns off FTS indexing for this library\n{status}\n    Shows the current indexing status\n{reindex}\n    Can be used to re-index either particular books or\n    the entire library. To re-index particular books\n    specify the book ids as additional arguments after the\n    {reindex} command. If no book ids are specified the\n    entire library is re-indexed.\n (format args: [])",
    "options": [
      {
        "_file": "cmd_fts_index.py",
        "default": false,
        "action": "store_true",
        "help": "Wait till all books are indexed, showing indexing progress periodically",
        "flags": [
          "--wait-for-completion"
        ]
      },
      {
        "_file": "cmd_fts_index.py",
        "default": "",
        "choices": "('fast', 'slow', '')",
        "help": "The speed of indexing. Use fast for fast indexing using all your computers resources and slow for less resource intensive indexing. Note that the speed is reset to slow after every invocation.",
        "flags": [
          "--indexing-speed"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_fts_search": {
    "file": "cmd_fts_search.py",
    "usage_text": "%prog fts_search [options] search expression\n\nDo a full text search on the entire library or a subset of it.\n\n",
    "options": [
      {
        "_file": "cmd_fts_search.py",
        "default": false,
        "action": "store_true",
        "help": "Include snippets of the text surrounding each match. Note that this makes searching much slower.",
        "flags": [
          "--include-snippets"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "default": "\u001b[31m",
        "help": "The marker used to indicate the start of a matched word inside a snippet",
        "flags": [
          "--match-start-marker"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "default": "\u001b[m",
        "help": "The marker used to indicate the end of a matched word inside a snippet",
        "flags": [
          "--match-end-marker"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "default": true,
        "dest": "use_stemming",
        "action": "store_false",
        "help": "Only match on exact words not related words. So correction will not match correcting.",
        "flags": [
          "--do-not-match-on-related-words"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "default": "",
        "help": "Restrict the searched books, either using a search expression or ids. For example: ids:1,2,3 to restrict by ids or search:tag:foo to restrict to books having the tag foo.",
        "flags": [
          "--restrict-to"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "default": "text",
        "choices": "('text', 'json')",
        "help": "The format to output the search results in. Either \"text\" for plain text or \"json\" for JSON output.",
        "flags": [
          "--output-format"
        ]
      },
      {
        "_file": "cmd_fts_search.py",
        "type": "float",
        "default": 90.0,
        "help": "How much of the library must be indexed before searching is allowed, as a percentage. Defaults to 90",
        "flags": [
          "--indexing-threshold"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_list": {
    "file": "cmd_list.py",
    "usage_text": "%prog list [options]\n\nList the books available in the calibre database.\n",
    "options": [
      {
        "_file": "cmd_list.py",
        "default": "title,authors",
        "help": "_('The fields to display when listing books in the database. Should be a comma separated list of fields.\\nAvailable fields: %s\\nDefault: %%default. The special field \"all\" can be used to select all fields. In addition to the builtin fields above, custom fields are also available as *field_name, for example, for a custom field #rating, use the name: *rating') % ', '.join(sorted(FIELDS))",
        "flags": [
          "-f",
          "--fields"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": null,
        "help": "The field by which to sort the results. You can specify multiple fields by separating them with commas.\nAvailable fields: ', '.join(sorted(FIELDS))\nDefault: id. In addition to the builtin fields above, custom fields are also available as *field_name, for example, for a custom field #rating, use the name: *rating",
        "flags": [
          "--sort-by"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": false,
        "action": "store_true",
        "help": "Sort results in ascending order",
        "flags": [
          "--ascending"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": null,
        "help": "Filter the results by the search query. For the format of the search query, please see the search related documentation in the User Manual. Default is to do no filtering.",
        "flags": [
          "-s",
          "--search"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": "-1",
        "type": "int",
        "help": "The maximum width of a single line in the output. Defaults to detecting screen size.",
        "flags": [
          "-w",
          "--line-width"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": " ",
        "help": "The string used to separate fields. Default is a space.",
        "flags": [
          "--separator"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": null,
        "help": "The prefix for all file paths. Default is the absolute path to the library folder.",
        "flags": [
          "--prefix"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": "-1",
        "type": "int",
        "help": "The maximum number of results to display. Default: all",
        "flags": [
          "--limit"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": false,
        "action": "store_true",
        "help": "Generate output in JSON format, which is more suitable for machine parsing. Causes the line width and separator options to be ignored.",
        "flags": [
          "--for-machine"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": null,
        "help": "The template to run if \"template\" is in the field list. Note that templates are ignored while connecting to a calibre server. Default: None",
        "flags": [
          "--template"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": null,
        "help": "Path to a file containing the template to run if \"template\" is in the field list. Default: None",
        "flags": [
          "--template_file",
          "-t"
        ]
      },
      {
        "_file": "cmd_list.py",
        "default": "template",
        "help": "Heading for the template column. Default: %default. This option is ignored if the option --for-machine is set",
        "flags": [
          "--template_heading"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_list_categories": {
    "file": "cmd_list_categories.py",
    "usage_text": "%prog list_categories [options]\n\nProduce a report of the category information in the database. The\ninformation is the equivalent of what is shown in the Tag browser.\n",
    "options": [
      {
        "_file": "cmd_list_categories.py",
        "default": false,
        "action": "store_true",
        "help": "Output only the number of items in a category instead of the counts per item within the category",
        "flags": [
          "-i",
          "--item_count"
        ]
      },
      {
        "_file": "cmd_list_categories.py",
        "default": false,
        "action": "store_true",
        "help": "Output in CSV",
        "flags": [
          "-c",
          "--csv"
        ]
      },
      {
        "_file": "cmd_list_categories.py",
        "default": "excel",
        "choices": "csv.list_dialects()",
        "help": "The type of CSV file to produce. Choices: ', '.join(sorted(csv.list_dialects()))",
        "flags": [
          "--dialect"
        ]
      },
      {
        "_file": "cmd_list_categories.py",
        "default": "",
        "dest": "report",
        "help": "Comma-separated list of category lookup names. Default: all",
        "flags": [
          "-r",
          "--categories"
        ]
      },
      {
        "_file": "cmd_list_categories.py",
        "default": "-1",
        "type": "int",
        "help": "The maximum width of a single line in the output. Defaults to detecting screen size.",
        "flags": [
          "-w",
          "--width"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_remove": {
    "file": "cmd_remove.py",
    "usage_text": "%prog remove ids\n\nRemove the books identified by ids from the database. ids should be a comma separated list of id numbers (you can get id numbers by using the search command). For example, 23,34,57-85 (when specifying a range, the last number in the range is not included).\n",
    "options": [
      {
        "_file": "cmd_remove.py",
        "default": false,
        "action": "store_true",
        "help": "Do not use the trash_name()",
        "flags": [
          "--permanent"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_remove_custom_column": {
    "file": "cmd_remove_custom_column.py",
    "usage_text": "%prog remove_custom_column [options] label\n\nRemove the custom column identified by label. You can see available\ncolumns with the custom_columns command.\n    ",
    "options": [
      {
        "_file": "cmd_remove_custom_column.py",
        "default": false,
        "action": "store_true",
        "help": "Do not ask for confirmation",
        "flags": [
          "-f",
          "--force"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_remove_format": {
    "file": "cmd_remove_format.py",
    "usage_text": "\n%prog remove_format [options] id fmt\n\nRemove the format fmt from the logical book identified by id. You can get id by using the search command. fmt should be a file extension like LRF or TXT or EPUB. If the logical book does not have fmt available, do nothing.\n",
    "options": [],
    "option_groups": []
  },
  "cmd_restore_database": {
    "file": "cmd_restore_database.py",
    "usage_text": "%prog restore_database [options]\n\nRestore this database from the metadata stored in OPF files in each\nfolder of the calibre library. This is useful if your metadata.db file\nhas been corrupted.\n\nWARNING: This command completely regenerates your database. You will lose\nall saved searches, user categories, plugboards, stored per-book conversion\nsettings, and custom recipes. Restored metadata will only be as accurate as\nwhat is found in the OPF files.\n    ",
    "options": [
      {
        "_file": "cmd_restore_database.py",
        "default": false,
        "action": "store_true",
        "help": "Really do the recovery. The command will not run unless this option is specified.",
        "flags": [
          "-r",
          "--really-do-it"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_saved_searches": {
    "file": "cmd_saved_searches.py",
    "usage_text": "%prog saved_searches [options] (list|add|remove)\n\nManage the saved searches stored in this database.\nIf you try to add a query with a name that already exists, it will be\nreplaced.\n\nSyntax for adding:\n\n%prog saved_searches add search_name search_expression\n\nSyntax for removing:\n\n%prog saved_searches remove search_name\n    ",
    "options": [],
    "option_groups": []
  },
  "cmd_search": {
    "file": "cmd_search.py",
    "usage_text": "%prog search [options] search expression\n\nSearch the library for the specified search term, returning a comma separated\nlist of book ids matching the search expression. The output format is useful\nto feed into other commands that accept a list of ids as input.\n\nThe search expression can be anything from calibre's powerful search query\nlanguage, for example: %prog search author:asimov 'title:\"i robot\"'\n",
    "options": [
      {
        "_file": "cmd_search.py",
        "default": "-1",
        "type": "int",
        "help": "The maximum number of results to return. Default is all results.",
        "flags": [
          "-l",
          "--limit"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_set_custom": {
    "file": "cmd_set_custom.py",
    "usage_text": "%prog set_custom [options] column id value\n\nSet the value of a custom column for the book identified by id.\nYou can get a list of ids using the search command.\nYou can get a list of custom column names using the custom_columns\ncommand.\n    ",
    "options": [
      {
        "_file": "cmd_set_custom.py",
        "default": false,
        "action": "store_true",
        "help": "If the column stores multiple values, append the specified values to the existing ones, instead of replacing them.",
        "flags": [
          "-a",
          "--append"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_set_metadata": {
    "file": "cmd_set_metadata.py",
    "usage_text": "\n%prog set_metadata [options] book_id [/path/to/metadata.opf]\n\nSet the metadata stored in the calibre database for the book identified by\nbook_id from the OPF file metadata.opf. book_id is a book id number from the\nsearch command. You can get a quick feel for the OPF format by using the\n--as-opf switch to the show_metadata command. You can also set the metadata of\nindividual fields with the --field option. If you use the --field option, there\nis no need to specify an OPF file.\n",
    "options": [
      {
        "_file": "cmd_set_metadata.py",
        "action": "append",
        "default": "[]",
        "help": "The field to set. Format is field_name:value, for example: --field tags:tag1,tag2. Use --list-fields to get a list of all field names. You can specify this option multiple times to set multiple fields. Note: For languages you must use the ISO639 language codes (e.g. en for English, fr for French and so on). For identifiers, the syntax is --field identifiers:isbn:XXXX,doi:YYYYY. For boolean (yes/no) fields use true and false or yes and no.",
        "flags": [
          "-f",
          "--field"
        ]
      },
      {
        "_file": "cmd_set_metadata.py",
        "action": "store_true",
        "default": false,
        "help": "List the metadata field names that can be used with the --field option",
        "flags": [
          "-l",
          "--list-fields"
        ]
      }
    ],
    "option_groups": []
  },
  "cmd_show_metadata": {
    "file": "cmd_show_metadata.py",
    "usage_text": "\n%prog show_metadata [options] id\n\nShow the metadata stored in the calibre database for the book identified by id.\nid is an id number from the search command.\n",
    "options": [
      {
        "_file": "cmd_show_metadata.py",
        "default": false,
        "action": "store_true",
        "help": "Print metadata in OPF form (XML)",
        "flags": [
          "--as-opf"
        ]
      }
    ],
    "option_groups": []
  },
  "extract_options": {
    "file": "extract_options.py",
    "usage_text": null,
    "options": [],
    "option_groups": []
  },
  "main": {
    "file": "main.py",
    "usage_text": "_('%%prog command [options] [arguments]\\n\\n%%prog is the command line interface to the calibre books database.\\n\\ncommand is one of:\\n  %s\\n\\nFor help on an individual command: %%prog command --help\\n') % '\\n  '.join(COMMANDS)",
    "options": [
      {
        "_file": "main.py",
        "default": null,
        "help": "Path to the calibre library. Default is to use the path stored in the settings. You can also connect to a calibre Content server to perform actions on remote libraries. To do so use a URL of the form: http://hostname:port/#library_id for example, http://localhost:8080/#mylibrary. library_id is the library id of the library you want to connect to on the Content server. You can use the special library_id value of - to get a list of library ids available on the server. For details on how to setup access via a Content server, see localize_user_manual_link('https://manual.calibre-ebook.com/generated/en/calibredb.html').",
        "flags": [
          "--library-path",
          "--with-library"
        ]
      },
      {
        "_file": "main.py",
        "help": "show this help message and exit",
        "action": "help",
        "flags": [
          "-h",
          "--help"
        ]
      },
      {
        "_file": "main.py",
        "help": "show program's version number and exit",
        "action": "version",
        "flags": [
          "--version"
        ]
      },
      {
        "_file": "main.py",
        "help": "Username for connecting to a calibre Content server",
        "flags": [
          "--username"
        ]
      },
      {
        "_file": "main.py",
        "help": "Password for connecting to a calibre Content server. To read the password from standard input, use the special value: <stdin>. To read the password from a file, use: '<f:C:/path/to/file>' if iswindows else '<f:/path/to/file>' (i.e. <f: followed by the full path to the file and a trailing >). The angle brackets in the above are required, remember to escape them or use quotes for your shell.",
        "flags": [
          "--password"
        ]
      },
      {
        "_file": "main.py",
        "type": "float",
        "default": 120,
        "help": "The timeout, in seconds, when connecting to a calibre library over the network. The default is two minutes.",
        "flags": [
          "--timeout"
        ]
      }
    ],
    "option_groups": []
  },
  "tests": {
    "file": "tests.py",
    "usage_text": null,
    "options": [],
    "option_groups": []
  },
  "utils": {
    "file": "utils.py",
    "usage_text": null,
    "options": [],
    "option_groups": []
  }
}